import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    container:{
        margin: 7,
        borderRadius: 40,
        borderWidth: 3,
        borderColor: 'violet'
    },
    image:{
        borderRadius: 40,
        borderWidth: 1 ,
        borderColor: 'violet'
    }
})

export default styles;